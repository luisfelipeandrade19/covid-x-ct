services:
  classificador:
    build: .
    volumes:
      - D:\Dataset:/app/data

    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]